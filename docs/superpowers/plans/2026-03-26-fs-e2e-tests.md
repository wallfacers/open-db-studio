# FS Abstraction E2E Tests — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `src/mcp/fs/e2e.test.ts` 新增 6 个 E2E 测试用例，覆盖事件 → useMcpBridge → FsRouter → QueryTabAdapter → store 的完整链路，验证 A 层 Integration 和 C 层 Scenario 行为。

**Architecture:** 单一测试文件，分两个 `describe` 块（A 层 Integration + C 层 Scenario），共享 mock 基础设施和辅助函数。关键挑战：`queryStore` 模块加载时自动执行 `loadTabsFromStorage()`，需要在 `mockImplementation` 配置后、store `setState` 前等待其完成。

**Tech Stack:** Vitest · @testing-library/react (renderHook) · Zustand (真实 store) · Tauri API mock

---

## 文件规划

| 操作 | 路径 | 说明 |
|------|------|------|
| Create | `src/mcp/fs/e2e.test.ts` | 全部 E2E 测试（唯一变更文件） |

---

## Chunk 1：基础设施（mock、辅助函数、beforeEach）

### Task 1：写测试骨架并验证可运行（0 个测试通过、0 个测试失败）

**Files:**
- Create: `src/mcp/fs/e2e.test.ts`

- [ ] **Step 1：创建空骨架文件**

```typescript
// src/mcp/fs/e2e.test.ts

import { describe, it, expect, beforeEach, vi } from 'vitest'
import { renderHook } from '@testing-library/react'
import { useMcpBridge } from '../../hooks/useMcpBridge'
import { useQueryStore } from '../../store/queryStore'
import { useAppStore } from '../../store/appStore'
import { invoke } from '@tauri-apps/api/core'

// ─── Mock Tauri APIs ─────────────────────────────────────────────────────────

vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn() }))

type ListenCallback<T> = (event: { payload: T }) => void | Promise<void>
const capturedListeners: Record<string, ListenCallback<unknown>> = {}

vi.mock('@tauri-apps/api/event', () => ({
  listen: vi.fn((eventName: string, cb: ListenCallback<unknown>) => {
    capturedListeners[eventName] = cb
    return Promise.resolve(() => { delete capturedListeners[eventName] })
  }),
  emit: vi.fn().mockResolvedValue(undefined),
}))

const mockInvoke = vi.mocked(invoke)

// ─── 辅助函数 ────────────────────────────────────────────────────────────────

function mountBridge(): void {
  renderHook(() => useMcpBridge())
}

async function emitFsRequest(
  op: string,
  resource: string,
  target: string,
  payload: Record<string, unknown>,
  request_id = 'test-req',
): Promise<void> {
  const handler = capturedListeners['mcp://query-request']
  await handler?.({
    payload: { request_id, query_type: 'fs_request', params: { op, resource, target, payload } },
  })
}

function lastRespondData(): unknown {
  const call = [...mockInvoke.mock.calls]
    .reverse()
    .find(c => c[0] === 'mcp_query_respond')
  return (call?.[1] as { data: unknown } | undefined)?.data
}

// ─── beforeEach ──────────────────────────────────────────────────────────────

beforeEach(async () => {
  vi.clearAllMocks()

  // 按命令分发 invoke mock
  // queryStore 模块加载时自动执行 loadTabsFromStorage，会调用这些命令
  mockInvoke.mockImplementation((cmd: string) => {
    if (cmd === 'list_tab_files') return Promise.resolve([])
    if (cmd === 'read_tab_file') return Promise.resolve(null)
    if (cmd === 'get_ui_state') return Promise.resolve(null)
    // write_tab_file、set_ui_state、mcp_query_respond 等均静默吸收
    return Promise.resolve(undefined)
  })

  // 等待 loadTabsFromStorage 完成（多级 await，用 setTimeout(0) 排到宏任务之后）
  await new Promise(r => setTimeout(r, 0))

  // 预置 store 状态（覆盖 loadTabsFromStorage 的空结果）
  useQueryStore.setState({
    tabs: [{ id: 'tab-q1', type: 'query', title: 'conn1/mydb', connectionId: 1, db: 'mydb' }],
    activeTabId: 'tab-q1',
    sqlContent: { 'tab-q1': 'SELECT * FROM users' },
    pendingDiff: null,
  })
  useAppStore.setState({ autoMode: false })
})

// ─── A 层：Integration ────────────────────────────────────────────────────────

describe('A: Integration — event → FsRouter → Adapter → store', () => {
  it.todo('A-1: fs_read 返回 SQL 内容')
  it.todo('A-2: fs_write 非 Auto → pendingDiff')
  it.todo('A-3: fs_write Auto → 直接写入')
})

// ─── C 层：Scenario ──────────────────────────────────────────────────────────

describe('C: Scenario — 完整 AI 工作流', () => {
  it.todo('场景1：非 Auto 改 SQL → 用户确认 → SQL 更新')
  it.todo('场景2：Auto 模式直接覆写')
  it.todo('场景3：搜索 tab → 打开新 tab')
})
```

- [ ] **Step 2：运行骨架，确认文件可编译（todo 不计入失败）**

```bash
npx vitest run src/mcp/fs/e2e.test.ts
```

期望：0 failed，6 todo（或 0 tests run，无报错）

- [ ] **Step 3：运行全量测试，确认现有 205 个用例无回归**

```bash
npx vitest run
```

期望：全部通过，无新增失败

- [ ] **Step 4：类型检查**

```bash
npx tsc --noEmit
```

期望：无新增错误

- [ ] **Step 5：Commit**

```bash
git add src/mcp/fs/e2e.test.ts
git commit -m "test(fs): add e2e test file skeleton with mock infrastructure"
```

---

## Chunk 2：A 层 Integration 测试

### Task 2：A-1 — fs_read 返回 SQL 内容

**Files:**
- Modify: `src/mcp/fs/e2e.test.ts`

- [ ] **Step 1：将 A-1 todo 替换为真实测试**

将文件中 `it.todo('A-1: fs_read 返回 SQL 内容')` 替换为：

```typescript
it('A-1: fs_read 返回 SQL 内容', async () => {
  mountBridge()
  await emitFsRequest('read', 'tab.query', 'active', { mode: 'text' }, 'a-1')

  const data = lastRespondData() as { content: string; lines: Array<{ no: number; text: string }> }
  expect(data.content).toBe('SELECT * FROM users')
  expect(data.lines).toEqual([{ no: 1, text: 'SELECT * FROM users' }])
  // 验证 mcp_query_respond 被调用且 requestId 正确
  const call = mockInvoke.mock.calls.find(c => c[0] === 'mcp_query_respond')
  expect(call?.[1]).toMatchObject({ requestId: 'a-1' })
})
```

- [ ] **Step 2：运行 A-1 用例**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "A-1"
```

期望：1 passed

- [ ] **Step 3：如果失败，诊断**

常见问题：
- `handler` 为 undefined → mountBridge 在 capturedListeners 被注册前调用，检查 listen mock 是否正确
- `data` 为 null → `loadTabsFromStorage` 覆盖了 setState，增加 `await new Promise(r => setTimeout(r, 0))` 的延迟时间（改为 10ms）
- `content` 不对 → 检查 `useQueryStore.setState` 中 `sqlContent['tab-q1']` 是否已设置

### Task 3：A-2 — fs_write 非 Auto → pendingDiff

- [ ] **Step 1：将 A-2 todo 替换为真实测试**

```typescript
it('A-2: fs_write 非 Auto → pendingDiff', async () => {
  // autoMode 已在 beforeEach 设为 false
  mountBridge()
  await emitFsRequest(
    'write', 'tab.query', 'active',
    { mode: 'text', op: 'replace_all', content: 'SELECT 1', reason: '优化' },
    'a-2',
  )

  const state = useQueryStore.getState()
  expect(state.pendingDiff).not.toBeNull()
  expect(state.pendingDiff!.original).toBe('SELECT * FROM users')
  expect(state.pendingDiff!.modified).toBe('SELECT 1')
  expect(state.pendingDiff!.reason).toBe('优化')

  const data = lastRespondData() as { status: string }
  expect(data.status).toBe('pending_confirm')
})
```

- [ ] **Step 2：运行 A-2 用例**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "A-2"
```

期望：1 passed

### Task 4：A-3 — fs_write Auto → 直接写入

- [ ] **Step 1：将 A-3 todo 替换为真实测试**

```typescript
it('A-3: fs_write Auto → 直接写入', async () => {
  useAppStore.setState({ autoMode: true })
  mountBridge()
  await emitFsRequest(
    'write', 'tab.query', 'active',
    { mode: 'text', op: 'replace_all', content: 'SELECT 1' },
    'a-3',
  )

  const state = useQueryStore.getState()
  expect(state.sqlContent['tab-q1']).toBe('SELECT 1')
  expect(state.pendingDiff).toBeNull()

  const data = lastRespondData() as { status: string }
  expect(data.status).toBe('applied')
})
```

- [ ] **Step 2：运行 A 层全部 3 个用例**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "A:"
```

期望：3 passed

- [ ] **Step 3：运行全量测试，确认无回归**

```bash
npx vitest run
```

期望：全部通过

- [ ] **Step 4：Commit**

```bash
git add src/mcp/fs/e2e.test.ts
git commit -m "test(fs): add A-layer integration tests for fs read/write via real chain"
```

---

## Chunk 3：C 层 Scenario 测试

### Task 5：Scenario 1 — 非 Auto 改 SQL → 用户确认 → SQL 更新

**Files:**
- Modify: `src/mcp/fs/e2e.test.ts`

- [ ] **Step 1：将场景1 todo 替换为真实测试**

```typescript
it('场景1：非 Auto 改 SQL → 用户确认 → SQL 更新', async () => {
  // autoMode 已在 beforeEach 设为 false
  mountBridge()

  // 步骤 1：AI 发起写操作
  await emitFsRequest(
    'write', 'tab.query', 'active',
    { mode: 'text', op: 'replace_all', content: 'SELECT id FROM users', reason: '只取 id' },
  )

  // 中间状态：pendingDiff 存在，SQL 尚未更新
  const mid = useQueryStore.getState()
  expect(mid.pendingDiff!.original).toBe('SELECT * FROM users')
  expect(mid.pendingDiff!.modified).toBe('SELECT id FROM users')
  expect(mid.sqlContent['tab-q1']).toBe('SELECT * FROM users')

  // 步骤 2：用户点击确认
  useQueryStore.getState().applyDiff()

  // 最终断言：SQL 已更新，pendingDiff 已清除
  const final = useQueryStore.getState()
  expect(final.sqlContent['tab-q1']).toBe('SELECT id FROM users')
  expect(final.pendingDiff).toBeNull()
})
```

- [ ] **Step 2：运行场景1**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "场景1"
```

期望：1 passed

### Task 6：Scenario 2 — Auto 模式直接覆写

- [ ] **Step 1：将场景2 todo 替换为真实测试**

```typescript
it('场景2：Auto 模式直接覆写', async () => {
  useAppStore.setState({ autoMode: true })
  mountBridge()

  await emitFsRequest(
    'write', 'tab.query', 'active',
    { mode: 'text', op: 'replace_all', content: 'SELECT id FROM users WHERE active=1' },
  )

  const state = useQueryStore.getState()
  expect(state.sqlContent['tab-q1']).toBe('SELECT id FROM users WHERE active=1')
  expect(state.pendingDiff).toBeNull()

  const data = lastRespondData() as { status: string }
  expect(data.status).toBe('applied')
})
```

- [ ] **Step 2：运行场景2**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "场景2"
```

期望：1 passed

### Task 7：Scenario 3 — 搜索 tab → 打开新 tab → 返回 tab_id

- [ ] **Step 1：将场景3 todo 替换为真实测试**

```typescript
it('场景3：搜索 tab → 打开新 tab', async () => {
  mountBridge()

  // 步骤 1：AI 搜索 tab（search 操作 target 不使用，传空字符串）
  await emitFsRequest('search', 'tab.query', '', { keyword: 'mydb' }, 'fs-s1')

  const searchResult = lastRespondData() as Array<{ resource: string; target: string }>
  expect(Array.isArray(searchResult)).toBe(true)
  expect(searchResult).toContainEqual(
    expect.objectContaining({ resource: 'tab.query', target: 'tab-q1' })
  )

  // 步骤 2：AI 打开新 tab
  const tabsBefore = useQueryStore.getState().tabs.length
  await emitFsRequest(
    'open', 'tab.query', '',
    { connection_id: 1, label: 'conn1', database: 'mydb' },
    'fs-s2',
  )

  const openResult = lastRespondData() as { target: string }
  const newTabId = openResult.target

  expect(typeof newTabId).toBe('string')
  expect(newTabId).not.toBe('tab-q1')
  expect(useQueryStore.getState().tabs.length).toBe(tabsBefore + 1)
  expect(
    useQueryStore.getState().tabs.find(t => t.id === newTabId)?.type
  ).toBe('query')
})
```

- [ ] **Step 2：运行场景3**

```bash
npx vitest run src/mcp/fs/e2e.test.ts -t "场景3"
```

期望：1 passed

### Task 8：验收 + 最终 Commit

- [ ] **Step 1：运行 e2e 文件全部 6 个用例**

```bash
npx vitest run src/mcp/fs/e2e.test.ts
```

期望：6 passed，0 failed，0 todo

- [ ] **Step 2：运行全量测试**

```bash
npx vitest run
```

期望：全部 211 个（原 205 + 新增 6）用例通过

- [ ] **Step 3：TypeScript 类型检查**

```bash
npx tsc --noEmit
```

期望：无新增类型错误

- [ ] **Step 4：最终 Commit**

```bash
git add src/mcp/fs/e2e.test.ts
git commit -m "test(fs): add E2E integration and scenario tests for FS abstraction layer

Covers event → useMcpBridge → FsRouter → QueryTabAdapter → store chain.
A-layer: read/write integration (3 tests).
C-layer: AI workflow scenarios including diff confirm flow (3 tests)."
```
